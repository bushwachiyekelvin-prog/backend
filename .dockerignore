# Ignore node modules and build outputs from the context (we install/build inside the image)
node_modules
.dist
/dist

# Git
.git
.gitignore

# Env files (supply via docker run or compose)
.env
.env.*
!.env.example

# IDE/editor
.vscode
.idea

# OS/Cache
.DS_Store
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Drizzle generated artifacts (optional to exclude)
# drizzle/  # Commented out - we need migration files for build
